{
  "version": 2,
  "builds": [
    {
      "src": "client/**",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "client/build",
        "buildCommand": "cd client && npm ci && NODE_ENV=production DISABLE_ESLINT_PLUGIN=true CI=false npm run build"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/client/build/$1" }
  ]
}